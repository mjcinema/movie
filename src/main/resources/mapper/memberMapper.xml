<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 
 <mapper namespace="ms"> 
	<insert id="memberjoin" parameterType="Member">
		INSERT INTO MJMEMBER (USERNAME , PASSWORD , MNAME , MBIRTH , MEMAIL , MADDR , MFILENAME)
		VALUES (#{username} , #{password} , #{mname} , #{mbirth} , #{memail} , #{maddr} , #{mfilename})
	</insert>
	
	<select id="memberlogin" parameterType="String" resultType="Member">
		SELECT USERNAME , PASSWORD , MNAME , MBIRTH , MEMAIL , MADDR , MFILENAME FROM MJMEMBER WHERE USERNAME=#{username}
	</select>
	
	<select id="idcheck" parameterType="String" resultType="Member">
		SELECT USERNAME FROM MJMEMBER WHERE USERNAME=#{username}
	</select>
	
	<select id="memberview" parameterType="String" resultType="Member">
		SELECT * FROM MJMEMBER WHERE USERNAME=#{username}
	</select>
	
	<select id="memberlist" resultType="Member">
		SELECT * FROM MJMEMBER
	</select>

	<update id="updateProfileImage" parameterType="Member">
		UPDATE MJMEMBER SET MFILENAME=#{mfilename} WHERE USERNAME=#{username}
	</update>

	<select id="memberlistview" parameterType="String" resultType="Member">
		SELECT * FROM MJMEMBER WHERE USERNAME=#{username}
	</select>
 </mapper>
 