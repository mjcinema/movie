<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="ms"> 
	<insert id="insertUser" parameterType="USER">
		INSERT INTO Users (MID , MPW , MNAME , MBIRTH , MEMAIL , MADDR , MFILENAME)
		VALUES (#{mid} , #{mpw} , #{mname} , #{mbirth} , #{memail} , #{maddr} , #{mfilename})
	</insert>
	
	<select id="selectUserByUsername" parameterType="String" resultType="Member">
		SELECT username, password, enabled, name, birth_date, email, address, image_name,  FROM 'USERS'
	</select>
	
	<select id="idcheck" parameterType="String" resultType="Member">
		SELECT MID FROM Users WHERE MID=#{mid}
	</select>
	
	<select id="memberview" parameterType="String" resultType="Member">
		SELECT * FROM Users WHERE MID=#{mid}
	</select>
	
	<select id="memberlist" resultType="Member">
		SELECT * FROM Users
	</select>

	<update id="updateProfileImage" parameterType="Member">
		UPDATE Users SET MFILENAME=#{mfilename} WHERE MID=#{mid}
	</update>
 </mapper>
 